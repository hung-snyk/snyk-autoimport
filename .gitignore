node_modules/
dist/
dist-test/
*.log
.DS_Store
.vscode/

# Internal design/decision log — not for distribution.
NOTES.md

# Agent instructions for working in this repo — kept local, not published.
CLAUDE.md

# Local credential store. Holds live Snyk and SCM tokens — never commit it.
.snyk-autoimport.json

# Test suite — kept local, not part of the distributed repository.
tests/
